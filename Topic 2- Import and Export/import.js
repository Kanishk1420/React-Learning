import ar  from "./number.js"; // this can be any name.
import nam from "./name.js"; 
import {arr, name} from "./namedexport.js"; // this should be same as the name of variable in namedexport.js file.
console.log(ar, nam);
console.log(arr, name);